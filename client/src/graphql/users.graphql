query User {
    users {
        id
        name
        age
    }
}

mutation AddUser($id: String!, $name: String!, $age: Int!) {
    addUser(id: $id, name: $name, age: $age){
        id
        name
        age
    }
}
